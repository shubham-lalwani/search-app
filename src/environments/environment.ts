const apiBaseURL: string = "http://localhost:3000";

export const environment = {
   countries_URL: apiBaseURL + "/countries",
   payments_URL: apiBaseURL + "/payments",
   users_URL: apiBaseURL + "/users",
}